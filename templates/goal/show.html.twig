{% extends 'base.html.twig' %}

{% block title %}Goal{% endblock %}

{% block body %}
    <h1>Goal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ goal.id }}</td>
            </tr>
            <tr>
                <th>Month</th>
                <td>{{ goal.month }}</td>
            </tr>
            <tr>
                <th>Limite</th>
                <td>{{ goal.limite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_goal_index') }}">back to list</a>

    <a href="{{ path('app_goal_edit', {'id': goal.id}) }}">edit</a>

    {{ include('goal/_delete_form.html.twig') }}
{% endblock %}
