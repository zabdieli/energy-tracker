<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Energy Tracker{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>
<body>
    
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">
            <i class="bi bi-lightning-charge-fill"></i> Energy Tracker
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" 
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('consumption_new') }}">
                        <i class="bi bi-plus-lg"></i> Saisie
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('consumption_history') }}">
                        <i class="bi bi-bar-chart-fill"></i> Historique
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_goal_index') }}">
                        <i class="bi bi-bullseye"></i> Objectifs
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('alerts') }}">
                        <i class="bi bi-bell-fill"></i> Alertes
                    </a>
                </li>
            </ul>
            {% if app.user %}
            <div class="dropdown">
                <a class="btn btn-outline-light dropdown-toggle" href="#" role="button" id="userMenu" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="bi bi-person-circle"></i> {{ app.user.email }}
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenu">
                    <li>
                        <form method="post" action="{{ path('app_logout') }}">
                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
                            <button class="dropdown-item text-danger" type="submit">
                                <i class="bi bi-box-arrow-right"></i> Se déconnecter
                            </button>
                        </form>
                    </li>
                </ul>
            </div>
            {% else %}
            <a class="btn btn-outline-light" href="{{ path('app_login') }}">
                <i class="bi bi-box-arrow-in-right"></i> Connexion
            </a>
            {% endif %}

        </div>
    </div>
</nav>

<div class="container mt-5 pt-4">
    {% block body %}{% endblock %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
