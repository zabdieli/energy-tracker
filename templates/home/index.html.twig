<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Energy Tracker{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            {# <a class="navbar-brand" href="{{ path('home') }}">⚡ Energy Tracker</a> #}
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="{{ path('consumption_new') }}">➕ Saisie</a></li>
                    {# <li class="nav-item"><a class="nav-link" href="{{ path('consumption_history') }}">📊 Historique</a></li> #}
                    {# <li class="nav-item"><a class="nav-link" href="{{ path('goals') }}">🎯 Objectifs</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ path('alerts') }}">🔔 Alertes</a></li> #}
                </ul>
                {% if app.user %}
                    <span class="navbar-text text-white">👋 Bonjour {{ app.user.email }}</span>
                {% endif %}
            </div>
        </div>
    </nav>

    <div class="container">
        {% block body %}{% endblock %}
    </div>
</body>
</html>
